chatroom
========

Example of a use for the Mediator pattern with a chatroom.

1. compile with CMake :

  ```
  $ mkdir build
  $ cd build
  $ cmake ..
  ```

2. make :

  ```$ make```

3. run :

  ```$ ./testChat```

